# string is a built in , immutable data type available in python.
# it supports index based accessing and slicling like list.
# supports built in functions and methods

a = 'PythonFun'
n = len(a)
for x in range(n):
    print(a[0:x+1])

print(a)
